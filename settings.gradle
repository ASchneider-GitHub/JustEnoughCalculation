pluginManagement {
    repositories {
        maven { url "https://maven.fabricmc.net/" }
        maven { url "https://maven.architectury.dev/" }
        maven { url "https://maven.minecraftforge.net/" }
        gradlePluginPortal()
    }
}

include("common")
include("fabric-like")
include("fabric")
include("neoforge")
// include("quilt") // out of scope for the 1.21.1 migration pass
// include("forge") // out of scope for the 1.21.1 migration pass

rootProject.name = "jecalculation"
